# Write an SQL query to display the number of employees in each department, sorted by the employee count in descending order.

---


SELECT dept, COUNT(*) AS total_employees
FROM employees
GROUP BY dept
ORDER BY total_employees DESC;
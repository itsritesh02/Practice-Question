# Write an SQL query to find the total number of employees in the employees table.

---

SELECT COUNT(*)
FROM employees;


# Agar column ka naam chahiye:

SELECT COUNT(*) AS total_employees
FROM employees;